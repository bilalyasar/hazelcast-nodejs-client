version.update:
    - "*.json"
create.artifacts:
        - npm install
        - npm publish
        - github_changelog_generator hazelcast/hazelcast-nodejs-client --unreleased-label LATEST_RELEASE
publish:
        - github:
            - description:CHANGELOG.md
